export default {
    base: "/odin-weather-app/"
  };

  /* If you update your app later, repeat npm run build and npm run deploy to push changes.
If you run into 404 errors, double-check the base path in vite.config.js. */